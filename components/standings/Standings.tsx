import {Category,Match,Registration,cats,groupRows,groups} from '@/lib/fixtures'
export function Standings({regs,matches}:{regs:Registration[],matches:Match[]}){return <div className="categoryStandings">{cats.map(cat=><CategoryStandings key={cat} cat={cat} regs={regs} matches={matches}/>)}</div>}
function CategoryStandings({cat,regs,matches}:{cat:Category,regs:Registration[],matches:Match[]}){return <section className="card"><h3>{cat}</h3>{groups[cat].map(g=><div className="miniGroup" key={`${cat}-${g}`}><h4>Grupo {g}</h4><Table rows={groupRows(cat,regs,matches,g)}/></div>)}</section>}
export function Table({rows}:{rows:any[]}){return <div className="table"><table><thead><tr><th>Slot</th><th>Pareja</th><th>Pts</th><th>PJ</th><th>PG</th><th>PP</th><th>J+</th><th>J-</th><th>DJ</th></tr></thead><tbody>{rows.map(r=><tr key={r.slot}><td>{r.slot}</td><td>{r.name}</td><td><b>{r.pts}</b></td><td>{r.pj}</td><td>{r.pg}</td><td>{r.pp}</td><td>{r.jf}</td><td>{r.jc}</td><td className={r.dj>0?'good':r.dj<0?'bad':''}>{r.dj}</td></tr>)}</tbody></table><p className="hint">DJ = diferencia de juegos · J+ = juegos a favor · J- = juegos en contra</p></div>}
